"""Per-site scrapers for the Tampere Keikat data pipeline. Each module here
is independent and knows about exactly one website; scrape.py orchestrates
them and doesn't know any site-specific parsing details itself."""
